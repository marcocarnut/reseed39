# Rebuildable wasm-toolchain artifacts (large; regenerate via wasm/build.sh
# and the gmp step documented in REPORT.md / wasm/build_gmp.sh).
wasm/build/
wasm/gmp-wasm/
node_modules/

# Local dev HTTP server runtime (P3 estimator page); details live in REPORT.md.
server.log
server.log.info

# Keep the built core committed for durability across box restarts:
#   wasm/rxecore.js, wasm/rxecore.wasm, and wasm/rxecore-single.js (the
#   SINGLE_FILE variant the SPA bundler inlines) are intentionally NOT ignored.

# The single-file SPA build output: regenerate with `node tools/bundle.mjs`.
dist/
